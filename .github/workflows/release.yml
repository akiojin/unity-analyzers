name: Release

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    outputs:
      release_created: ${{ steps.release.outputs.release_created }}
      tag_name: ${{ steps.release.outputs.tag_name }}
      version: ${{ steps.release.outputs.version }}
    steps:
      - name: Run release-please
        id: release
        uses: googleapis/release-please-action@v4
        with:
          token: ${{ secrets.PERSONAL_ACCESS_TOKEN }}

  build-and-publish:
    needs: release-please
    if: ${{ needs.release-please.outputs.release_created }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: main
          token: ${{ secrets.PERSONAL_ACCESS_TOKEN }}

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore dependencies
        run: dotnet restore UnityAnalyzer/Packages/com.akiojin.unity.analyzers/Analyzers~/StrictRules.Analyzers.csproj

      - name: Build analyzer
        run: dotnet build UnityAnalyzer/Packages/com.akiojin.unity.analyzers/Analyzers~/StrictRules.Analyzers.csproj --configuration Release --no-restore

      - name: Copy DLL to Plugins
        run: |
          mkdir -p UnityAnalyzer/Packages/com.akiojin.unity.analyzers/Plugins
          cp UnityAnalyzer/Packages/com.akiojin.unity.analyzers/Analyzers~/bin/Release/netstandard2.0/StrictRules.Analyzers.dll UnityAnalyzer/Packages/com.akiojin.unity.analyzers/Plugins/

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Commit updated DLL
        run: |
          git add UnityAnalyzer/Packages/com.akiojin.unity.analyzers/Plugins/StrictRules.Analyzers.dll
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "chore: update analyzer DLL for release ${{ needs.release-please.outputs.tag_name }}"
            git push origin main
          fi

      - name: Create package archive
        run: |
          mkdir -p dist
          cd UnityAnalyzer/Packages/com.akiojin.unity.analyzers
          tar -czf ../../../dist/com.akiojin.unity.analyzers-${{ needs.release-please.outputs.version }}.tgz \
            --exclude='.git*' \
            --exclude='Analyzers~' \
            --exclude='dist' \
            --exclude='*.meta' \
            package.json \
            Plugins/ \
            Editor/

      - name: Upload package to release
        env:
          GITHUB_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
        run: |
          gh release upload ${{ needs.release-please.outputs.tag_name }} \
            dist/com.akiojin.unity.analyzers-${{ needs.release-please.outputs.version }}.tgz \
            --clobber
